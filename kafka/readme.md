🧠 What this gives you

Kafka broker (on localhost:9092)

Zookeeper (required for Kafka < 3.x)

Schema Registry (for Avro/Protobuf JSON schemas)

Kafka UI (super clean GUI to inspect messages & partitions)

Access Kafka UI here:
👉 http://localhost:8080

Schema Registry here:
👉 http://localhost:8081


commands:

* docker-compose up -d
* docker logs kafka -f
* docker-compose down

